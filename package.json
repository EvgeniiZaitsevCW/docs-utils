{
  "name": "docs-utils",
  "version": "1.0.0",
  "description": "Doc utils project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "categorize_addresses": "npx tsc && node dist/categorize_addresses.js",
    "normalize_addresses": "npx tsc && node dist/normalize_addresses.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.11.18",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "ethers": "^5.7.2"
  }
}
